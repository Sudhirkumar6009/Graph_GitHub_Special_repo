{
  "name": "github-activity-graph",
  "version": "1.0.0",
  "description": "GitHub contribution activity graph rendered as SVG, served via Vercel Serverless Functions",
  "scripts": {
    "dev": "ts-node server.ts",
    "start": "ts-node server.ts",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  }
}
